@page
@model SOH.Web.Pages.Post.DetailsModel

@{
    ViewData["Title"] = "Details";
    ViewData["NavClass"] = "top-nav-collapse";
}

@section Styles
    {
    <style>
        body {
            padding-top: 100px;
        }
    </style>
}

<div class="container">
    <div class="text-center py-5 font-weight-bold">
        <h2>@Model.Post.Title</h2>
        <p class="text-muted font-italic">@Model.Post.Created.Day/@Model.Post.Created.Month/@Model.Post.Created.Year</p>
        <p class="lead">
            @Model.Post.Content
        </p>
        @if (@Model.Post.Image != null)
        {
            <img src="@Model.Post.Image.URI" />
        }
    </div>

    <!--More stories-->
    <div class="">

    </div>
</div>

@*<div class="container">
        <h2>Details</h2>

        <div>
            <h4>Post</h4>
            <hr />
            <dl class="dl-horizontal">
                <dt>
                    @Html.DisplayNameFor(model => model.Post.Title)
                </dt>
                <dd>
                    @Html.DisplayFor(model => model.Post.Title)
                </dd>
                <dt>
                    @Html.DisplayNameFor(model => model.Post.Content)
                </dt>
                <dd>
                    @Html.DisplayFor(model => model.Post.Content)
                </dd>
                <dt>
                    @Html.DisplayNameFor(model => model.Post.Created)
                </dt>
                <dd>
                    @Html.DisplayFor(model => model.Post.Created)
                </dd>
                <dt>
                    @Html.DisplayNameFor(model => model.Post.LastAccessed)
                </dt>
                <dd>
                    @Html.DisplayFor(model => model.Post.LastAccessed)
                </dd>
                <dt>
                    @Html.DisplayNameFor(model => model.Post.ApplicationUserId)
                </dt>
                <dd>
                    @Html.DisplayFor(model => model.Post.ApplicationUserId)
                </dd>
                <dt>
                    @Html.DisplayNameFor(model => model.Post.Image)
                </dt>
                <dd>
                    @Html.DisplayFor(model => model.Post.Image.Id)
                </dd>
            </dl>
        </div>
        <div>
            <a asp-page="./Edit" asp-route-id="@Model.Post.Id">Edit</a> |
            <a asp-page="./Index">Back to List</a>
        </div>
    </div>*@